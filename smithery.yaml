startCommand:
  type: stdio
  configSchema:
    properties:
      timezone:
        type: string
        title: "Default Timezone"
        description: "Select the timezone used for dates and times returned by this server."
        enum:
          - "America/New_York"
          - "America/Chicago"
          - "America/Denver"
          - "America/Los_Angeles"
          - "America/Sao_Paulo"
          - "Europe/London"
          - "Europe/Paris"
          - "Asia/Dubai"
          - "Asia/Kolkata"
          - "Asia/Singapore"
          - "Asia/Shanghai"
          - "Asia/Tokyo"
          - "Australia/Sydney"
          - "UTC"
        default: "America/New_York"
    required:
      - timezone
  commandFunction: |
    (config) => ({
      command: "python",
      args: ["server.py"],
      env: { DEFAULT_TIMEZONE: config.timezone }
    })
